import React from 'react';

const Profile = () => (
    <div>Profile Module</div>
);

export default {
    routeProps: {
        path: '/profile',
        component: Profile,
    },
    name: 'Profile',
};
